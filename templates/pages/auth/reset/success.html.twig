{% extends 'layouts/public.html.twig' %}

{% block title %}
    Reset Password
{% endblock %}

{% block content %}
    {% if result == 'requested' %}
        <p>
            Wir haben deine Anfrage erhalten. Falls die E-Mail-Adresse <strong>{{ email }}</strong>
            bei uns hinterlegt ist, erhälst du eine E-Mail mit einem Link zum Zurücksetzen
            eines Passworts.
        </p>
        <p>
            Bitte beachte, dass dieser Link nur <strong>eine Stunde gültig</strong> ist!
        </p>
        <hr />
        <p>
            E-Mail nicht erhalten? Schaue in deinem Spam-Ordner nach oder
            <a href="{{ path('app_auth_reset_request') }}">versuche es nochmal.</a>
        </p>
        <p>
            Your can remember? <a href="{{ path('app_auth_login') }}">Login</a> :)
        </p>
    {% elseif result == 'password_changed' %}
        <div class="alert alert-success mb-4" role="alert">
            Dein Passwort wurde erfolgreich geändert.
        </div>
        {% if is_guest() %}
            <p>
                Du kannst dich mit deinem neuen Password einloggen:
            </p>
            <a href="{{ path('app_auth_login') }}" class="btn-outline-primary btn-lg w-100">
                Zum Login
            </a>
        {% else %}
            <p>
                Zur Startseite:
            </p>
            <a href="{{ path('root') }}" class="btn-outline-primary btn-lg w-100">
                Home
            </a>
        {% endif %}
    {% endif %}
{% endblock %}
